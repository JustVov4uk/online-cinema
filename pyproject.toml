[tool.poetry]
name = "onlinecinema"
version = "0.1.0"
description = ""
authors = ["JustVov4uk <volodabudzan4@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.136.3"
uvicorn = "^0.49.0"


[tool.poetry.group.dev.dependencies]
pytest = "^9.1.0"
httpx = "^0.28.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
pythonpath = ["."]
testpath = ["tests"]
